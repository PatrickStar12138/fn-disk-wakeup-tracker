# 一、文件作用与适用范围

本文件是当前仓库的强制开发约束，适用于：

- Codex
- 其他 AI 编程代理
- 自动化代码生成工具
- 当前仓库中的所有开发人员

本文件位于仓库根目录，对整个仓库及其所有子目录生效。

除非子目录存在更具体的 `AGENTS.md`，否则必须完整遵守本文件。

子目录中的 `AGENTS.md` 只能增加更严格的约束，不得降低本文件的安全、质量、测试、注释和交付要求。

本文件中的要求属于交付验收条件，不是建议。

如果用户当前任务与本文件存在冲突：

1. 优先遵守用户当前明确提出的要求。
2. 不得违反数据安全、系统安全、路径安全和禁止破坏性操作的约束。
3. 发现冲突时必须明确说明。
4. 不得自行选择高风险方案。
5. 不得通过绕过检查、删除测试或伪造结果解决冲突。

---

# 二、项目基本信息

当前项目：

- 项目名称：`fn-disk-wakeup-tracker`
- 中文名称：`硬盘唤醒追踪器`
- 应用类型：飞牛 fnOS 原生应用
- 后端语言：Go
- 前端技术：Vue 3 + TypeScript + Vite
- 数据库：SQLite
- SQLite 驱动：优先使用纯 Go 驱动，避免 CGO
- 运行方式：fnOS 原生应用，不使用 Docker
- 接入方式：优先使用 fnOS 官方统一网关和 Unix Socket
- 支持架构：Linux amd64、Linux arm64
- 版本来源：仓库根目录 `VERSION`
- Git 提交语言：简体中文
- 代码注释语言：简体中文

项目开发必须参考飞牛官方开发文档：

```text
https://developer.fnnas.com/docs/guide/
````

涉及以下内容时，不得依靠记忆或旧项目经验猜测：

* manifest
* privilege
* resource
* 生命周期脚本
* 统一网关
* Unix Socket
* 用户向导
* 安装向导
* 卸载向导
* 应用入口
* 图标规范
* fnpack
* appcenter-cli
* 应用数据目录
* 环境变量
* 应用升级
* 应用卸载
* 数据保留机制

官方文档与旧项目代码冲突时，以当前官方文档为准。

---

# 三、最高优先级原则

任何开发任务都必须遵循以下原则：

1. 不修改无关代码。
2. 不删除无关文件。
3. 不格式化整个仓库。
4. 不擅自重构用户未要求的模块。
5. 不引入与当前任务无关的新功能。
6. 不通过临时硬编码掩盖问题。
7. 不通过删除测试让构建通过。
8. 不通过忽略错误让构建通过。
9. 不伪造测试结果。
10. 不伪造真机验证结果。
11. 不声称执行了实际没有执行的命令。
12. 不声称生成了实际不存在的 FPK。
13. 不声称修复了尚未验证的问题。
14. 不提交密码、Token、Cookie、私钥或测试账号。
15. 不进行强制推送。
16. 不擅自合并主分支。
17. 不擅自修改 Git 历史。
18. 不绕过 fnOS 官方权限模型。
19. 不使用固定公网端口绕过统一网关。
20. 不让 Web 服务长期以 root 身份运行。
21. 不允许任何用户输入直接拼接为 Shell 命令。
22. 不删除任何非本应用拥有的数据。
23. 不因监控行为主动唤醒机械硬盘。
24. 不将推测结果描述为确定事实。
25. 不把 `unknown` 状态显示成 `standby`。

---

# 四、开始任务前的检查流程

收到任务后，必须先检查：

```bash
git status
git branch --show-current
git log -5 --oneline
```

同时检查：

* 当前分支
* 工作区是否干净
* 是否存在未提交修改
* 是否存在用户正在进行的其他改动
* 项目目录结构
* 根目录 `VERSION`
* 根目录及子目录中的 `AGENTS.md`
* 构建脚本
* 测试脚本
* manifest
* 生命周期脚本
* 最近相关提交
* 是否存在同名或重复实现

发现未提交修改时：

* 不得覆盖。
* 不得执行 `git reset --hard`。
* 不得执行 `git clean -fd`。
* 不得擅自暂存用户修改。
* 必须区分本次修改和已有修改。
* 修改文件前必须确认不会破坏现有工作。

开始实现前，需要输出简短计划，至少说明：

1. 准备修改哪些文件。
2. 准备新增哪些文件。
3. 核心实现方式。
4. 可能风险。
5. 准备执行的测试。
6. 是否涉及版本、数据库或生命周期脚本。

计划完成后直接继续实施，不要只输出方案后停止。

---

# 五、修改范围约束

每次任务必须遵守最小修改原则。

允许修改：

* 与当前任务直接相关的文件
* 当前任务必须依赖的公共代码
* 当前任务对应的测试
* 当前任务对应的文档
* 当前任务对应的构建脚本

禁止修改：

* 无关模块
* 无关页面
* 无关样式
* 无关 API
* 其他项目目录
* 用户未要求调整的命名
* 用户未要求升级的依赖
* 用户未要求重构的公共组件

不得以以下理由扩大修改范围：

* “顺便优化”
* “代码风格更统一”
* “未来可能会用到”
* “这个方案更先进”
* “重构后更好看”
* “先把技术债一起清理”

确实发现严重问题时：

* 先记录在最终总结中。
* 除非会直接阻止当前任务，否则不要扩大修改。
* 安全漏洞和数据损坏风险可以立即修复，但必须说明原因和影响。

---

# 六、中文注释强制规范

## 6.1 总体要求

所有新增和修改的方法、函数必须添加准确的简体中文注释。

中文注释属于交付验收条件。

出现以下任一情况，视为任务未完成：

* 新增方法没有中文注释
* 修改方法后注释未更新
* 注释与实际实现不一致
* 关键复杂逻辑没有说明
* 使用大量无意义注释
* 用纯英文注释替代中文注释
* 通过缩短代码规避注释要求
* 只给导出方法加注释，忽略内部方法

## 6.2 必须添加方法注释的代码

包括但不限于：

* Go 函数
* Go 方法
* HTTP Handler
* Service 方法
* Repository 方法
* 数据库访问方法
* 数据迁移方法
* Collector 方法
* Provider 方法
* 状态机方法
* 配置加载方法
* 权限校验方法
* 诊断方法
* 工具方法
* 测试方法
* 测试辅助方法
* Vue 组件方法
* TypeScript 函数
* TypeScript 箭头函数
* Hooks
* Store 方法
* API 请求方法
* 事件处理函数
* Shell 函数
* 构建脚本函数
* 生命周期脚本函数

不得以“方法很简单”“方法名已经很清楚”为理由省略注释。

## 6.3 方法注释内容

方法注释根据复杂程度，应说明：

1. 方法用途。
2. 主要处理逻辑。
3. 关键参数含义。
4. 返回值含义。
5. 错误情况。
6. 副作用。
7. 并发行为。
8. 是否写数据库。
9. 是否访问硬盘。
10. 是否可能唤醒硬盘。
11. 是否执行外部命令。
12. 是否需要管理员权限。

简单方法可以使用一行准确注释。

复杂方法必须使用多行注释。

## 6.4 Go 注释规范

导出的 Go 标识符注释必须以标识符名称开头，满足 `go vet` 和 golint 类规则。

正确示例：

```go
// DetectDiskState 检测指定物理磁盘当前的运行状态。
//
// 该方法优先使用不会主动唤醒机械硬盘的检测方式。
// 当系统不支持无损检测时返回 unknown，不允许通过读取磁盘文件强制获取状态。
//
// 参数：
//   - ctx：用于控制检测超时和任务取消。
//   - device：经过系统扫描和白名单校验的磁盘设备。
//
// 返回值：
//   - DiskState：磁盘当前状态。
//   - error：检测失败时返回具体错误。
func DetectDiskState(ctx context.Context, device DiskDevice) (DiskState, error) {
    // ...
}
```

内部方法示例：

```go
// mergePendingEvents 合并同一硬盘在短时间内产生的重复状态事件，避免频繁写入 SQLite。
func mergePendingEvents(events []DiskEvent) []DiskEvent {
    // ...
}
```

禁止：

```go
// 检测状态
func DetectDiskState() {
}
```

导出方法注释不能缺少方法名称。

## 6.5 TypeScript 注释规范

所有新增或修改的 TypeScript 函数必须添加中文注释。

示例：

```ts
/**
 * 加载硬盘状态总览。
 *
 * 请求后端总览接口并更新页面状态。
 * 请求失败时清除加载状态并展示明确错误，禁止继续将旧数据伪装为最新数据。
 */
async function loadDashboard(): Promise<void> {
  // ...
}
```

箭头函数同样需要注释：

```ts
/**
 * 将后端磁盘状态转换为页面展示文字。
 */
const formatDiskState = (state: DiskState): string => {
  // ...
};
```

模板中的简单内联表达式不强制添加注释，但复杂逻辑不得直接堆在模板中，必须提取为带中文注释的方法。

## 6.6 Shell 注释规范

Shell 中所有函数必须添加中文注释：

```bash
# 安全停止应用服务。
#
# 先发送正常终止信号并等待进程退出，
# 超过规定时间后才执行强制终止。
stop_service() {
    # ...
}
```

生命周期脚本中的危险操作必须添加原因和保护逻辑说明。

## 6.7 类型和结构注释

以下内容必须有中文注释：

* struct
* interface
* type
* enum
* 常量组
* 配置结构
* API 请求对象
* API 响应对象
* 数据库实体
* 状态枚举
* 错误类型
* 数据迁移结构
* 前端 TypeScript interface
* 前端 TypeScript type
* Pinia Store
* Vue 组件的重要 Props 和 Emits

示例：

```go
// DiskState 表示物理磁盘当前的运行状态。
//
// unknown 表示当前无法可靠检测，不能等同于 standby。
type DiskState string
```

## 6.8 字段注释

以下字段必须添加中文注释：

* 配置字段
* 数据库字段
* API 字段
* 时间字段
* 大小字段
* 数量字段
* 带单位字段
* 布尔字段
* 枚举字段
* 容易混淆的字段
* 安全相关字段

必须明确单位：

```go
type CollectorConfig struct {
    // SampleIntervalSeconds 表示状态采样间隔，单位为秒。
    SampleIntervalSeconds int `json:"sampleIntervalSeconds"`

    // EventRetentionDays 表示历史事件保留天数。
    EventRetentionDays int `json:"eventRetentionDays"`

    // DatabaseMaxSizeMB 表示 SQLite 数据库允许的最大体积，单位为 MB。
    DatabaseMaxSizeMB int `json:"databaseMaxSizeMB"`

    // RecordLowConfidence 表示是否持久化低可信度的疑似唤醒来源。
    RecordLowConfidence bool `json:"recordLowConfidence"`
}
```

## 6.9 关键逻辑行内注释

以下逻辑必须添加中文行内注释：

* 状态机转换
* 状态防抖
* 首次启动处理
* Collector 重启恢复
* 并发控制
* goroutine 生命周期
* channel 关闭
* 锁处理
* 超时处理
* SQLite 事务
* 数据迁移
* 数据清理
* 批量写入
* 有界重试
* Unix Socket 权限处理
* 管理员鉴权
* Collector 和 Server 通信
* 硬盘状态检测
* 避免主动唤醒硬盘
* 进程与磁盘 I/O 关联
* 可信度计算
* 卸载路径安全校验
* 命令白名单
* 输入校验
* 降级逻辑
* 兼容逻辑
* 特殊正则表达式
* 非直观算法

示例：

```go
// 连续检测到待机状态达到确认次数后才转换状态，避免瞬时抖动产生虚假休眠事件。
if candidateCount >= requiredConfirmations {
    currentState = DiskStateStandby
}
```

## 6.10 禁止无意义注释

禁止：

```go
// 获取数据
// 设置值
// 执行处理
// 判断是否为空
// 遍历列表
// 返回结果
// 创建对象
// 调用方法
```

禁止逐行翻译代码：

```go
// count 加一
count++
```

注释应重点解释：

* 为什么这样实现
* 为什么不能使用其他方式
* 有什么限制
* 有什么风险
* 如何避免误判
* 如何避免数据损坏
* 如何避免唤醒硬盘
* 如何处理兼容性问题

## 6.11 修改旧代码

修改现有方法时必须：

1. 检查原注释是否准确。
2. 原注释不准确时同步更新。
3. 原方法无中文注释时补充。
4. 重命名方法时同步更新注释。
5. 修改参数时同步更新参数说明。
6. 修改返回值时同步更新返回说明。
7. 修改副作用时同步更新风险说明。
8. 删除方法时删除失效注释。
9. 不保留与实现冲突的历史说明。

## 6.12 测试代码注释

每个测试方法必须说明：

* 测试场景
* 输入条件
* 预期结果
* 防止哪类问题回归

示例：

```go
// TestStateMachine_FirstActiveDoesNotCreateWakeEvent 验证应用首次启动时，
// 即使检测到磁盘处于活动状态，也不会错误生成“磁盘刚刚被唤醒”事件。
func TestStateMachine_FirstActiveDoesNotCreateWakeEvent(t *testing.T) {
    // ...
}
```

表驱动测试需要对每组复杂场景添加清晰名称。

## 6.13 TODO 规范

禁止模糊 TODO：

```go
// TODO 后续处理
```

允许：

```go
// TODO(v0.2.0)：真机验证 USB-SATA 桥接设备在 standby 状态下是否支持无损检测，
// 当前版本返回 unknown，避免执行可能唤醒硬盘的探测命令。
```

TODO 必须说明：

* 原因
* 当前处理方式
* 后续目标
* 预计版本或触发条件

---

# 七、中文注释自动检查

必须维护：

```bash
./scripts/check-comments.sh
```

该脚本至少检查：

* 新增 Go 函数是否存在中文注释
* 修改 Go 函数是否存在中文注释
* 导出 Go 类型是否存在规范注释
* TypeScript 导出函数是否存在中文注释
* Vue 组件中的复杂方法是否存在中文注释
* Shell 函数是否存在中文注释
* 测试方法是否存在场景说明
* 是否出现明显的纯英文方法注释
* 是否出现无说明的 TODO
* 是否出现大量无意义注释

注释检查必须集成到：

```bash
./scripts/build.sh
make test
make release
```

注释检查失败时：

* 构建必须失败。
* 不得跳过。
* 不得注释掉检查脚本。
* 不得降低规则以规避问题。
* 不得提交。

自动检查只能辅助，不能替代人工检查注释是否准确。

---

# 八、代码质量规范

## 8.1 通用要求

所有代码必须：

* 结构清晰
* 命名准确
* 单一职责
* 错误处理完整
* 边界条件明确
* 避免重复实现
* 避免超长方法
* 避免超深嵌套
* 避免魔法数字
* 避免全局可变状态
* 避免隐式副作用
* 避免无界队列
* 避免无限重试
* 避免永久阻塞
* 避免吞掉错误

不得仅为了减少文件数量，把所有代码放进一个文件。

不得仅为了“架构漂亮”引入大量无实际价值的抽象层。

## 8.2 方法长度

复杂方法应拆分，但不得机械拆分成大量无意义的小方法。

建议：

* 普通方法尽量不超过 80 行。
* 超过 100 行必须检查是否需要拆分。
* 超过 150 行必须说明无法拆分的原因。
* Handler 不得同时承担参数解析、业务处理、数据库访问和响应拼装。

## 8.3 错误处理

禁止：

```go
value, _ := doSomething()
```

除非确实安全忽略，并且必须添加中文说明。

错误必须：

* 保留上下文
* 使用 `%w` 包装原始错误
* 避免泄露敏感信息
* 返回用户可理解的信息
* 日志保留排障信息
* 不重复记录同一错误多次

禁止：

* 捕获后什么都不做
* 只返回“系统异常”
* 把完整数据库错误直接返回前端
* 把系统路径和敏感环境变量直接返回前端

## 8.4 并发规范

所有 goroutine 必须明确：

* 谁创建
* 谁取消
* 谁等待
* 谁关闭 channel
* 如何防止泄漏
* 如何处理 panic
* 如何处理应用停止

必须使用：

* `context.Context`
* 明确的超时
* `sync.WaitGroup` 或等价生命周期管理
* 有界 channel
* 有界工作池

禁止：

* 无限制创建 goroutine
* 多方关闭同一个 channel
* 后台 goroutine 无法停止
* 在锁内执行长时间 I/O
* 持锁执行外部命令
* 无限等待 SQLite
* Collector 重载时重复启动采集循环

## 8.5 日志规范

日志必须：

* 有级别
* 有模块
* 有上下文
* 有必要的事件 ID
* 有大小限制
* 有保留数量限制
* 支持滚动
* 避免高频重复刷屏

禁止记录：

* 密码
* Token
* Cookie
* 私钥
* 完整请求头
* 完整环境变量
* 未脱敏序列号
* 用户隐私数据
* 大量原始请求体

高频采样失败必须聚合或限流，不得每次轮询都写一条相同日志。

---

# 九、Go 后端规范

必须执行：

```bash
gofmt
go test ./...
go vet ./...
```

根据项目配置执行：

```bash
go mod tidy
```

但不得无理由升级所有依赖。

要求：

* 使用明确的包职责。
* 禁止循环依赖。
* API、Service、Repository、Provider 分层清晰。
* 不在 Handler 中直接编写复杂 SQL。
* 不在数据库层混入 HTTP 逻辑。
* 不在业务层直接读取任意系统路径。
* 系统访问必须通过受控 Provider。
* 外部命令必须通过统一执行器。
* 所有外部命令必须设置超时。
* 所有设备参数必须经过白名单校验。
* 不使用 `sh -c` 拼接用户输入。
* 不直接信任前端传入的设备名称。
* 设备必须来自服务端扫描结果。
* Server 不得长期以 root 运行。
* Collector 如需 root 权限，必须与 Server 隔离。
* Collector 不得暴露网络端口。

---

# 十、前端规范

## 10.1 技术约束

前端使用：

* Vue 3
* TypeScript
* Vite
* Composition API

所有依赖必须本地打包。

禁止：

* CDN
* 在线字体
* 远程图标
* 运行时访问公网资源
* 在生产包中包含 source map，除非明确需要
* 在前端保存管理员权限标记作为安全判断依据

前端权限判断只用于界面展示，后端必须再次校验。

## 10.2 TypeScript

禁止随意使用：

```ts
any
```

确实无法避免时必须添加中文说明。

要求：

* API 请求和响应有明确类型。
* 枚举状态使用联合类型或 enum。
* 不直接使用魔法字符串。
* 时间、大小和状态转换集中处理。
* 错误状态与空状态区分。
* 加载状态与刷新状态区分。
* 不吞掉 Promise 错误。
* 组件卸载时取消未完成请求。

必须执行：

```bash
npm run type-check
npm run test
npm run build
```

以项目实际脚本为准。

## 10.3 UI 交互

页面必须适配 fnOS iframe。

支持宽度：

* 1440
* 1280
* 1024
* 768

要求：

* 不横向溢出。
* 不出现遮挡。
* 不出现按钮超出容器。
* 窄窗口导航可收起。
* 表格窄屏有合理处理。
* 弹窗不能超出窗口。
* 加载时显示骨架屏或明确加载状态。
* 空数据必须显示空状态。
* 请求失败必须显示错误状态。
* 不允许请求失败后继续把旧数据伪装成最新数据。
* 刷新按钮有加载动画。
* 刷新成功更新时间。
* Toast 缓慢滑入并淡入。
* Toast 停留后平滑淡出并收回。
* 不允许提示突然出现或消失。
* 保存按钮防重复提交。
* 危险操作二次确认。
* 颜色不能是状态的唯一表达方式。
* `unknown` 不得显示为绿色成功状态。
* 图表无数据时不得生成假曲线。
* 时间统一按用户本地时区展示。
* API 返回时间内部使用明确标准格式。

## 10.4 UI 风格

本项目使用现代可观测性控制台风格。

视觉要求：

* 清晰
* 克制
* 专业
* fnOS 原生窗口风格
* 支持浅色模式
* 支持深色模式
* 中性色背景
* 少量蓝色或青绿色强调色
* 合理留白
* 清晰层级
* 不堆叠大量同尺寸卡片
* 不使用大面积高饱和渐变
* 不使用廉价玻璃拟态
* 不使用过重阴影
* 不机械复制 UPS 项目 UI

可以参考 UPS 项目的交互逻辑，但不得复制已有问题和不合理布局。

---

# 十一、API 规范

所有 API 使用统一前缀：

```text
/api/v1
```

要求：

* 使用明确 HTTP 方法。
* GET 不执行修改操作。
* 修改操作进行管理员鉴权。
* 请求参数后端校验。
* 枚举值后端校验。
* 数值范围后端校验。
* 请求体限制大小。
* 导出接口流式输出。
* 列表接口必须分页。
* 禁止一次返回无上限历史数据。
* 错误响应结构统一。
* 不在错误响应中泄露 SQL、系统路径或内部堆栈。
* API 字段命名保持稳定。
* 修改 API 时同步更新前端类型和文档。
* 破坏性变更必须升级 API 或提供兼容迁移。

建议统一响应结构：

```json
{
  "success": true,
  "data": {},
  "error": null,
  "requestId": "..."
}
```

错误响应必须提供可定位的错误代码，但不得暴露敏感实现细节。

---

# 十二、管理员鉴权

本项目属于系统硬件管理工具，默认仅管理员可访问。

通过 fnOS 官方统一网关获取并校验用户信息。

不得仅依赖前端隐藏菜单。

后端必须验证官方网关提供的信息，例如：

* `X-Trim-Userid`
* `X-Trim-Isadmin`
* `X-Trim-Username`

具体字段和行为必须以当前官方文档为准。

禁止：

* 信任浏览器自行提交的管理员标志
* 信任 query 参数中的 userId
* 自己伪造 fnOS 用户会话
* 绕过统一网关建立另一套弱鉴权
* 因开发方便开放无鉴权接口
* 把 Unix Socket 权限设置为所有用户可写

---

# 十三、硬盘检测安全约束

本项目最高优先级要求：

> 监控应用本身不得因为状态检测、日志、数据库写入或 SMART 查询，持续阻止机械硬盘休眠。

必须遵守：

1. 优先使用不会主动唤醒硬盘的检测方式。
2. 无法可靠检测时返回 `unknown`。
3. 不得为了显示结果强制读取硬盘文件。
4. 不得为了判断休眠状态执行可能启动盘片的完整扫描。
5. 不得默认对待机盘执行完整 SMART 查询。
6. 只有确认磁盘已经活动时，才允许读取详细 SMART 数据。
7. SSD 和 NVMe 应显示“不适用”或相应能力状态。
8. 不把 SSD/NVMe 显示为机械盘待机状态。
9. 不把 `unknown` 当成 `standby`。
10. 不把相关性描述为确定因果。
11. UI 必须使用“疑似来源”。
12. 来源判断必须保留证据。
13. 必须标记可信度：高、中、低。
14. 应用自身进程 I/O 必须排除或单独标记。
15. 不高频遍历所有 PID 的全部文件描述符。
16. 进程扫描必须限制频率和范围。
17. 外部命令必须设置超时。
18. 不支持的环境必须明确显示“不支持”。
19. 不得通过模拟数据伪装检测成功。
20. 硬件行为必须支持 mock 测试。

---

# 十四、状态机规范

磁盘状态统一为：

```text
active
idle
standby
unknown
unsupported
```

状态转换必须满足：

* 首次启动不生成虚假唤醒事件。
* `standby -> active` 可靠检测后记录唤醒事件。
* `active -> standby` 可以连续多次确认。
* 相同状态不得重复写事件。
* 短暂抖动不得产生大量事件。
* Collector 重启不得将所有活动盘记录为新唤醒。
* 临时检测失败应进入 `unknown`，不得立即认定设备离线。
* USB 拔出与普通休眠区分。
* USB 重新接入与普通唤醒区分。
* 系统时间回拨不得产生负持续时间。
* 内部时间间隔优先使用单调时钟。
* 多块硬盘同时变化必须独立处理。
* 状态机逻辑必须有单元测试。

状态变化必须通过统一状态机处理，禁止多个模块分别直接写状态事件。

---

# 十五、SQLite 规范

SQLite 数据库必须：

* 使用 migration 管理版本。
* 不得启动时删除重建。
* migration 可重复检测。
* migration 失败保留原数据库。
* 外键明确。
* 唯一约束明确。
* 必要索引明确。
* 使用单写入者或受控写入队列。
* 写入队列必须有界。
* 数据库忙时有界重试。
* 不允许无限等待锁。
* 批量写入使用事务。
* 高频采样数据不逐条写入。
* 状态无变化时不重复写入。
* 日志表和事件表有保留策略。
* 数据清理分批执行。
* 清理任务不能高频运行。
* API 查询必须分页。
* 导出必须流式读取。
* 不一次性加载全部历史数据到内存。
* 数据库关闭必须等待写入任务结束。
* 升级前保护原始数据库。

至少包含：

```text
schema_migrations
disks
disk_capabilities
disk_state_events
wake_events
attribution_evidence
daily_statistics
settings
diagnostic_runs
operation_logs
```

表结构调整必须：

1. 新增 migration。
2. 增加 migration 测试。
3. 更新数据库文档。
4. 更新升级说明。
5. 验证旧数据兼容性。

---

# 十六、性能与资源约束

应用必须适合 NAS 长期运行。

要求：

* 高频状态只保存在内存环形缓冲区。
* 不允许每次采样都写 SQLite。
* 只在状态变化、事件产生或低频聚合时持久化。
* 日志必须限速和滚动。
* 数据库默认保留周期有限。
* 数据库默认大小有上限。
* API 分页有最大页大小。
* 导出使用流式处理。
* 不创建无界 goroutine。
* 不创建无界 channel。
* 不创建无界缓存。
* 定时任务必须可停止。
* 应用停止时等待任务退出。
* 不在每次请求时重新扫描整个系统。
* 不在每次刷新时执行完整硬件诊断。
* 不对不变数据重复解析和写入。
* 采样间隔必须有合理下限。
* 前端轮询必须在页面隐藏或卸载时暂停。

默认配置必须偏保守，避免监控工具自身产生大量 I/O。

---

# 十七、外部命令执行规范

所有外部命令必须集中封装。

必须满足：

* 固定可执行文件
* 固定参数模板
* 参数白名单
* 设备名白名单
* 超时控制
* 并发限制
* 输出大小限制
* 明确退出码处理
* 明确“不支持”处理
* 不记录敏感数据
* 不将完整原始错误返回前端

禁止：

```go
exec.Command("sh", "-c", userInput)
```

禁止：

* 前端传入任意命令
* 前端传入任意路径
* 前端传入任意设备名后直接执行
* 拼接 Shell 字符串
* 无超时执行命令
* 用 root Collector 执行任意操作
* 提供通用 Shell 调试接口

设备名称必须来自后端扫描结果，并再次验证格式和存在性。

---

# 十八、fnOS 原生应用架构

必须遵循官方应用结构。

禁止自创无法被官方工具识别的目录。

生命周期脚本至少根据实际需求包括：

```text
cmd/main
cmd/install_init
cmd/install_callback
cmd/upgrade_init
cmd/upgrade_callback
cmd/uninstall_init
cmd/uninstall_callback
cmd/config_init
cmd/config_callback
```

具体文件是否必需，以官方当前文档为准。

必须使用官方环境变量：

* `TRIM_APPDEST`
* `TRIM_PKGETC`
* `TRIM_PKGVAR`
* `TRIM_PKGTMP`
* `TRIM_PKGHOME`
* `TRIM_TEMP_LOGFILE`
* `TRIM_OLD_APPVER`

不得硬编码：

```text
/var/apps/fn-disk-wakeup-tracker
/vol1
/vol2
```

生命周期脚本要求：

* 使用 `#!/bin/bash`
* 使用 `set -u`
* 谨慎使用 `set -e`
* 所有路径变量使用双引号
* 可重复执行
* 有明确退出码
* 启动前检查重复进程
* 停止时等待正常退出
* 超时后才安全终止
* 不使用模糊 `pkill -f`
* PID 文件必须验证进程身份
* 清理 Unix Socket 前确认归属
* 停止前安全关闭 SQLite
* 用户可见错误写入官方临时日志
* 不因缺少可选能力导致整个安装失败

`status` 行为：

* 正常运行返回 0
* 未运行返回 3
* 异常状态返回其他明确错误码

---

# 十九、进程和权限模型

建议拆分：

## Collector

负责：

* 读取 `/proc`
* 读取 `/sys`
* 检测块设备
* 采集磁盘状态
* 采集受控进程 I/O
* 执行白名单硬件查询

Collector 如确实需要，可以以受控特权运行。

Collector 必须：

* 不暴露 TCP 端口
* 不接受网络请求
* 不执行任意命令
* 不接受任意路径
* 不接受任意设备名
* 只通过受保护 Unix Socket 或受控文件与 Server 通信

## Server

负责：

* 统一网关
* 静态页面
* API
* 管理员鉴权
* 查询 SQLite
* 配置管理
* 报告导出

Server 必须以应用专用低权限用户运行。

不得因为实现简单，让整个 Web 服务长期以 root 身份运行。

---

# 二十、统一网关和 Unix Socket

优先使用 fnOS 官方统一网关。

建议入口：

```text
/app/fn-disk-wakeup-tracker
```

后端使用 Unix Socket：

```text
${TRIM_APPDEST}/app.sock
```

具体配置以官方文档为准。

要求：

* 不额外监听 `0.0.0.0` 固定端口。
* Unix Socket 设置最小必要权限。
* 启动前清理本应用残留 Socket。
* 不删除其他应用 Socket。
* 前端正确处理子路径。
* Vite base 配置不得写死根路径。
* API 路径不得因 iframe 子路径失效。
* 静态资源路径必须兼容统一网关。
* 刷新子路由不得出现 404。
* 直接访问 API 不得绕过管理员校验。

---

# 二十一、版本号统一

根目录：

```text
VERSION
```

是唯一版本来源。

禁止在以下位置分别手改版本：

* manifest
* Go 常量
* 前端 package.json
* UI 关于页面
* API 版本接口
* FPK 文件名
* release 目录
* CHANGELOG
* 诊断报告

构建时从 `VERSION` 注入或校验：

* manifest version
* Go version
* UI version
* `/api/v1/version`
* FPK 文件名
* 构建摘要
* 诊断报告
* CHANGELOG 当前版本

必须维护：

```bash
./scripts/check-version.sh
```

发现不一致时构建失败。

版本格式必须符合：

```text
MAJOR.MINOR.PATCH
```

例如：

```text
0.1.0
```

Go 构建信息通过 `-ldflags` 注入：

* version
* commit
* buildTime
* platform

不得写死构建时间或提交号。

---

# 二十二、构建和打包

必须支持一键构建：

```bash
./scripts/build.sh all
./scripts/build.sh x86
./scripts/build.sh arm
make release
```

构建流程至少包括：

1. 检查工具。
2. 读取 VERSION。
3. 校验版本格式。
4. 检查版本一致性。
5. 检查中文注释。
6. 安装前端依赖。
7. 前端类型检查。
8. 前端测试。
9. 前端构建。
10. Go 格式检查。
11. Go 单元测试。
12. `go vet`。
13. 构建 Linux amd64。
14. 构建 Linux arm64。
15. 创建独立 staging 目录。
16. 复制官方 FPK 结构。
17. 注入对应 platform。
18. 注入版本。
19. 复制前端静态资源。
20. 检查脚本执行权限。
21. 检查 JSON 文件。
22. 检查 manifest。
23. 检查图标尺寸。
24. 调用 fnpack。
25. 生成 FPK。
26. 生成 SHA256。
27. 输出构建摘要。

推荐产物：

```text
fn-disk-wakeup-tracker-x86-0.1.0.fpk
fn-disk-wakeup-tracker-arm-0.1.0.fpk
SHA256SUMS
```

禁止把以下内容打入 FPK：

* `.git`
* `.env`
* node_modules
* 测试数据库
* 开发日志
* 调试文件
* 私钥
* Token
* Cookie
* 编辑器配置缓存
* macOS 临时文件
* 用户本地路径
* 源代码中无必要的构建缓存

构建过程中不得修改源码文件。

---

# 二十三、双架构要求

包含 Go 二进制时，必须分别构建：

```text
linux/amd64
linux/arm64
```

不得将包含单架构二进制的包声明为全平台通用。

x86 和 ARM 必须使用独立 staging 目录，避免二进制串包。

打包后必须检查：

```bash
file <binary>
```

确认：

* x86 包包含 x86-64 二进制
* ARM 包包含 aarch64 二进制

不得仅根据文件名判断架构。

---

# 二十四、安装、升级和卸载

## 24.1 安装

安装流程必须：

* 校验必要目录。
* 创建配置目录。
* 创建数据目录。
* 设置最小权限。
* 初始化默认配置。
* 检查旧数据。
* 不覆盖已存在的有效配置。
* 执行数据库 migration。
* 启动服务。
* 验证服务状态。
* 安装失败给出明确错误。

## 24.2 升级

升级必须：

* 读取旧版本。
* 保护现有配置。
* 保护历史数据库。
* 执行 migration。
* migration 失败保留原始数据库。
* 不删除历史数据。
* 不重置用户设置。
* 不重复生成默认配置覆盖旧配置。
* 记录升级日志。
* 支持回滚分析。

不得通过删除数据库解决升级兼容问题。

## 24.3 卸载

卸载必须提供：

```text
保留配置和历史数据
删除配置和历史数据
```

默认选择：

```text
保留配置和历史数据
```

选择保留时：

* 停止 Server。
* 停止 Collector。
* 等待 SQLite 安全关闭。
* 保留配置。
* 保留数据库。
* 保留历史事件。
* 保存数据格式版本。
* 重新安装后识别旧数据。
* 必要时自动 migration。

选择删除时，只允许删除：

* 本应用配置
* 本应用数据库
* 本应用事件
* 本应用日志
* 本应用临时文件
* 本应用诊断报告

禁止删除：

* 用户普通文件
* 用户自定义目录
* 其他应用目录
* 系统日志
* 系统配置
* 共享目录
* 磁盘挂载点

执行删除前必须验证：

* 路径非空
* 路径绝对规范化
* 路径属于本应用允许目录
* 路径不是 `/`
* 路径不是 `/var`
* 路径不是 `/home`
* 路径不是 `/vol1`
* 路径不是 `/vol2`
* 路径不是 `/vol*`
* 路径不是其他应用目录
* 不包含 `..`
* 不来自未经校验的用户输入

禁止：

```bash
rm -rf "$UNVERIFIED_PATH"
```

卸载删除路径校验必须有自动测试。

---

# 二十五、测试要求

任何功能必须同时考虑测试。

## 25.1 Go 测试

至少覆盖：

* 配置读取
* 配置默认值
* 配置范围校验
* 版本读取
* 状态机
* 首次启动
* 防抖
* 唤醒事件
* 休眠事件
* Collector 重启
* 硬盘拔出
* 硬盘重新接入
* 系统时间回拨
* 多盘同时变化
* SQLite migration
* SQLite 写入失败
* SQLite busy 重试
* 数据保留
* 数据清理
* 分页
* 权限中间件
* 管理员校验
* 设备名校验
* 命令白名单
* 命令超时
* 可信度计算
* 诊断报告脱敏
* 卸载路径保护

## 25.2 前端测试

至少覆盖：

* 加载状态
* 空数据
* API 错误
* unknown 状态
* unsupported 状态
* 分页
* 筛选
* 设置保存
* 保存失败
* 防重复提交
* Toast 进入
* Toast 退出
* 小窗口布局
* 版本展示
* 管理员权限错误
* 图表无数据

## 25.3 生命周期测试

使用模拟 `TRIM_*` 环境变量测试：

* install
* repeated install
* start
* repeated start
* status
* stop
* repeated stop
* upgrade
* uninstall keep
* uninstall delete
* 空路径保护
* 错误路径保护
* PID 文件失效
* Socket 残留
* 数据库未关闭
* 进程停止超时

测试不得真的访问或唤醒开发机机械硬盘。

硬件 Provider 必须可 mock。

## 25.4 回归要求

修复 Bug 时必须增加回归测试。

不能只修改实现，不补测试。

如果问题无法自动测试，必须：

* 说明原因。
* 补充手工验收步骤。
* 在最终总结中列为待真机验证。

---

# 二十六、真机验证要求

以下内容必须真机验证后才能声称通过：

* FPK 安装
* FPK 卸载
* fnOS 桌面入口
* 统一网关
* Unix Socket
* 管理员访问
* 普通用户拒绝
* x86 安装
* ARM 安装
* 硬盘休眠检测
* 硬盘唤醒事件
* SSD/NVMe 不适用状态
* USB 硬盘拔插
* 数据保留卸载
* 数据删除卸载
* 重新安装恢复数据
* fnOS 重启后自启动
* Collector 权限
* Server 降权
* 应用自身是否阻止硬盘休眠

没有真机环境时，必须标记：

```text
待真机验证
```

禁止写：

```text
已验证通过
```

最关键验收：

> 应用运行期间，不应因为自身高频轮询、SQLite 写入、日志写入或状态查询，持续阻止机械硬盘进入待机。

需要对比：

* 应用未运行时休眠表现
* 应用运行时休眠表现
* 应用进程 I/O
* 数据库写入频率
* 日志写入频率
* 状态命令调用频率

---

# 二十七、诊断报告和隐私

诊断报告必须脱敏。

禁止输出：

* 密码
* Token
* Cookie
* 私钥
* 完整环境变量
* 完整用户名列表
* 完整磁盘序列号
* 完整系统敏感路径
* 用户文件内容
* 数据库中的隐私数据

允许输出：

* 脱敏序列号
* 应用版本
* fnOS 版本
* 内核版本
* CPU 架构
* 服务状态
* 数据库大小
* 能力检测结果
* 最近错误摘要
* 当前非敏感配置

脱敏逻辑必须有测试。

---

# 二十八、依赖管理

新增依赖前必须评估：

* 是否必要
* 是否仍在维护
* 是否支持 amd64
* 是否支持 arm64
* 是否依赖 CGO
* 是否引入外部运行时
* 是否大幅增加 FPK 体积
* 是否存在明显安全风险
* 是否与现有依赖重复

禁止：

* 为一个简单函数引入大型框架
* 无理由升级全部依赖
* 引入公网运行时依赖
* 使用 CDN
* 引入必须安装 Node/Python 才能运行的后端组件
* 把包管理缓存打入 FPK

更新依赖时必须同步更新锁文件。

---

# 二十九、文档要求

任何功能变更必须考虑更新：

* README.md
* CHANGELOG.md
* API 文档
* 构建文档
* 安装文档
* 升级文档
* 卸载文档
* 真机验收文档
* 配置说明
* 已知限制

README 至少说明：

* 项目作用
* 支持平台
* 安装方式
* 构建方式
* 权限说明
* 数据目录
* 版本机制
* 卸载保留行为
* 应用不会主动修改硬盘休眠设置
* 应用自身可能产生的有限 I/O
* 当前已知限制
* 真机验证状态

不得在文档中写入尚未实现的功能，除非明确标记为规划。

---

# 三十、Git 分支和提交规范

## 30.1 分支

功能分支建议：

```text
feature/0.1.0-initial
feature/0.2.0-xxx
fix/xxx
refactor/xxx
```

不得：

* 直接强制修改主分支历史
* 未经允许合并主分支
* 未经允许 push
* 强制 push
* 删除远程分支
* 重写用户提交

## 30.2 中文提交信息

所有 Git 提交必须使用简体中文。

禁止：

```text
update
fix
changes
wip
test
final
done
修改
优化
修复问题
```

提交信息必须明确说明修改内容。

推荐格式：

```text
初始化：创建飞牛原生应用目录和构建框架
构建：实现统一版本管理和双架构一键打包
功能：实现物理磁盘扫描和能力识别
功能：实现磁盘状态采集与状态机
功能：实现唤醒事件持久化和统计
功能：实现疑似唤醒来源分析
功能：实现管理员鉴权和统一网关接口
界面：完成硬盘唤醒追踪器控制台
安全：限制硬件命令参数和设备访问范围
功能：实现卸载数据保留和删除选项
测试：补充状态机和数据库迁移测试
文档：补充安装升级卸载和真机验收说明
```

每个提交必须：

* 主题单一
* 内容可独立理解
* 对应测试通过
* 不混入无关修改
* 不提交密钥和本地配置
* 不提交无关构建产物

## 30.3 提交前检查

提交前必须执行：

```bash
git diff --check
./scripts/check-comments.sh
./scripts/check-version.sh
go test ./...
go vet ./...
```

同时执行前端和项目实际定义的测试。

测试失败不得提交。

---

# 三十一、禁止使用的 Git 命令

除非用户明确要求，禁止执行：

```bash
git reset --hard
git clean -fd
git clean -fdx
git checkout -- .
git restore .
git push --force
git push --force-with-lease
git rebase --onto
git filter-branch
git filter-repo
```

不得用这些命令清理用户未提交修改。

需要撤销本次修改时，只能精确恢复本次涉及的文件，并确认不会覆盖用户已有修改。

---

# 三十二、完成任务前的自检

完成任务前必须逐项检查：

## 代码

* 是否只修改了相关文件
* 是否存在临时硬编码
* 是否存在无用代码
* 是否存在调试输出
* 是否存在未处理错误
* 是否存在 goroutine 泄漏
* 是否存在无限重试
* 是否存在无界队列
* 是否存在未校验路径
* 是否存在未校验设备名
* 是否存在 Shell 拼接
* 是否存在敏感信息
* 是否存在高频 SQLite 写入
* 是否可能主动唤醒硬盘

## 注释

* 所有新增方法是否有中文注释
* 所有修改方法是否更新注释
* 所有类型是否有中文说明
* 所有关键字段是否标明单位
* 所有复杂逻辑是否有行内说明
* 是否存在无意义注释
* 是否存在纯英文方法注释
* TODO 是否说明原因和计划

## 版本

* VERSION 是否正确
* manifest 是否一致
* Go 版本是否一致
* UI 版本是否一致
* FPK 文件名是否一致
* CHANGELOG 是否一致

## 测试

* Go 测试是否执行
* go vet 是否执行
* 前端类型检查是否执行
* 前端测试是否执行
* 生命周期测试是否执行
* 构建脚本是否执行
* 打包是否执行
* SHA256 是否生成
* 哪些项目待真机验证

## Git

* Git diff 是否只包含相关修改
* 提交信息是否为中文
* 是否存在无关文件
* 是否提交了构建缓存
* 是否提交了敏感信息
* 是否擅自 push 或合并

任何一项未完成，必须在最终总结中明确说明。

---

# 三十三、最终回复格式

完成任务后必须按以下结构汇报：

## 实现总结

说明完成了什么。

## 修改文件

列出主要新增和修改文件。

## 核心实现

说明架构和关键逻辑。

## 中文注释检查

```text
新增方法数量：
已添加中文注释数量：
修改方法数量：
已更新注释数量：
注释检查命令：
注释检查结果：
```

没有实际统计时不得伪造数字。

## 版本检查

```text
VERSION：
manifest：
后端：
前端：
FPK：
检查结果：
```

## 执行的测试

逐条列出实际执行的命令和结果。

## 未执行的测试

说明未执行原因。

## 构建产物

列出实际存在的产物路径和 SHA256。

没有产物时必须明确说明未生成。

## 真机待验证

列出需要在 fnOS 真机完成的项目。

## 已知限制

列出当前仍存在的限制。

## Git 信息

```text
当前分支：
提交列表：
工作区状态：
是否推送：
```

不得只回复“已完成”。

---

# 三十四、明确禁止虚假完成

以下行为严禁出现：

* 没执行测试却写“测试通过”
* 没生成 FPK 却写“打包完成”
* 没真机验证却写“安装正常”
* 没检查注释却写“所有方法均有注释”
* 没检查版本却写“版本已统一”
* 没检查 Git 状态却写“工作区干净”
* 用模拟数据冒充硬件检测结果
* 用静态返回值冒充后端功能
* 为通过测试删除断言
* 将失败命令隐藏不报
* 将警告描述为成功
* 自动降低验收标准

如果某项失败，应如实说明：

* 执行了什么
* 失败在哪里
* 可能原因
* 当前影响
* 下一步建议

---

# 三十五、当前项目专项禁止事项

本项目禁止：

1. 使用 Docker。
2. 修改 fnOS 系统硬盘休眠配置。
3. 修改系统 `smartd` 配置。
4. 修改系统级 `hdparm` 配置。
5. 修改系统 systemd 服务。
6. 自动调整硬盘待机时间。
7. 自动执行磁盘修复。
8. 自动挂载磁盘。
9. 自动卸载磁盘。
10. 自动停止其他应用。
11. 自动停止 Docker 容器。
12. 自动杀死疑似唤醒进程。
13. 自动封禁用户。
14. 提供任意 Shell 执行。
15. 高频写 SQLite。
16. 高频写日志。
17. 高频扫描全部进程文件描述符。
18. 因查询状态主动唤醒磁盘。
19. 把疑似来源写成确定来源。
20. 把未知状态写成待机状态。
21. 硬编码 fnOS 安装路径。
22. 绕过统一网关暴露固定端口。
23. 让 Web Server 长期以 root 运行。
24. 删除非本应用数据。
25. 修改仓库内其他项目。
26. 机械复制 UPS 项目的全部 UI。
27. 擅自扩大首版功能范围。
28. 虚构已完成的硬件和真机测试。

首版只负责：

* 监控
* 记录
* 统计
* 疑似来源分析
* 诊断
* 报告导出

首版不负责自动修复或自动干预系统。

---

# 三十六、执行指令

接到任务后，必须按照以下顺序执行：

1. 阅读本文件。
2. 检查是否存在子目录 `AGENTS.md`。
3. 检查 Git 状态和当前分支。
4. 阅读相关现有代码。
5. 阅读相关官方 fnOS 文档。
6. 输出简短实施计划。
7. 按最小修改范围实现。
8. 为所有新增和修改的方法补充准确中文注释。
9. 补充或更新测试。
10. 执行注释检查。
11. 执行版本检查。
12. 执行代码格式和静态检查。
13. 执行单元测试。
14. 执行前端检查。
15. 执行构建。
16. 需要时执行 FPK 打包。
17. 检查 Git diff。
18. 按阶段使用中文提交信息提交。
19. 不执行 push，除非用户明确要求。
20. 按规定格式如实汇报结果。

任何方法缺少中文注释，或者注释与实际实现不一致，都视为任务未完成，不得打包、不得提交、不得声称已经完成。

````
